[package]
name = "rust-rocket-todo"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = { version = "0.5", features = ["json"] }
diesel = { version = "2.0", features = ["sqlite"] }
bcrypt = "0.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"